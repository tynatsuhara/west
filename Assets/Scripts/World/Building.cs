using UnityEngine;

// Represents a building exterior
public class Building : MonoBehaviour {
    public int width;
    public int height;
    public Vector2 doorTilePosition;  // where to build the road to
}